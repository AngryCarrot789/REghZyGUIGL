package reghzy.guigl.core.style.utils.base;

/**
 * A style is something that simply has a name, which can be used to link a value to (somewhere else)
 */
public interface Style {
    String getName();
}
